#!/usr/bin/zsh
#
function jupyter_kill() {
    printf -v JUP_PID "`jupyter server list --json | grep 9888 | jq '.pid'`"
	if ps -o pid --no-headers -fp $JUP_PID > /dev/null 2>&1; then
		pyenv shell jupyter
		jupyter server stop 9888
	fi
}
