# Setup fzf
# ---------
if [[ ! "$PATH" == */home/ngorse/.fzf/bin* ]]; then
  export PATH="${PATH:+${PATH}:}/home/ngorse/.fzf/bin"
fi

# Auto-completion
# ---------------
[[ $- == *i* ]] && source "/home/ngorse/.fzf/shell/completion.zsh" 2> /dev/null

# Key bindings
# ------------
source "/home/ngorse/.fzf/shell/key-bindings.zsh"
