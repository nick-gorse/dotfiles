
#export PYENV_VIRTUALENV_DISABLE_PROMPT=1
#eval "$(pyenv init -)"

#export PATH=$HOME/.pyenv/shims:$PATH
#export PYENV_VERSION=modin

# Added by GitButler installer
export PATH="/Users/ngorse/.local/bin:$PATH"
eval "$(but completions bash)"
